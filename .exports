export TERM="xterm-256color"
export EDITOR=vi

#sets the history to remove dups, store 10,000 entries and append on quit
export HISTCONTROL=erasedups
export HISTSIZE=10000
shopt -s histappend

#set vi as the command line editor
set -o vi

